{
  "name": "polishdata",
  "version": "0.0.1",
  "description": "Polish public data — KRS, CEIDG, REGON, White List VAT, insolvency",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "dependencies": {
    "eudata-common": "^0.0.1"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "license": "MIT",
  "files": ["dist"]
}
