{
  "name": "slovakdata",
  "version": "0.0.1",
  "description": "Slovak public data — ORSF, insolvency, VAT, debtor lists, courts",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "dependencies": {
    "eudata-common": "^0.0.1"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "license": "MIT",
  "files": ["dist"]
}
